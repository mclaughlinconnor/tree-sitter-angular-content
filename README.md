# Tree-sitter-angular_content

Will parse HTML tag content to extract `{{interpolation}}`.

## Developing

How to run & test:

```
npm install -g tree-sitter-cli
tree-sitter generate && tree-sitter test
```
